<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.environment.mapper.PersonMapper">
	
	<select id="getPersonListByDeptId" parameterType="Integer" resultType="com.atguigu.environment.model.Person">
		select id,name,email,salary from person where departmentId=#{departmentId}
	</select>
</mapper>


















